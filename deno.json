{
  "name": "@typek/signalhead",
  "version": "0.0.39",
  "exports": "./src/mod.ts",
  "tasks": {
    "test": "deno test --clean --coverage && deno coverage --lcov --output=lcov.info",
    "format": "deno lint --fix && prettier -w src"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/expect": "jsr:@std/expect@^1.0.0",
    "@typek/typek": "jsr:@typek/typek@^0.6.1"
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  }
}
